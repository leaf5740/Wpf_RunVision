<Window x:Class="Wpf_RunVision.Views.SchemeConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:views="clr-namespace:Wpf_RunVision.Views.TabViews"
        xmlns:vm="clr-namespace:Wpf_RunVision.ViewModels" 
        xmlns:views1="clr-namespace:Wpf_RunVision.ViewModels.TabViewModels"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Title="方案配置窗口" Height="700" Width="1200">

    <Window.DataContext>
        <vm:SchemeConfigViewModel/>
    </Window.DataContext>

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Closed">
            <i:InvokeCommandAction Command="{Binding WindowClosedCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <hc:TabControl Grid.Row="1" x:Name="TabControlDemo" 
                       ShowContextMenu="False" 
                       IsAnimationEnabled="True" 
                       ShowCloseButton="False" 
                       IsDraggable="True" 
                       IsTabFillEnabled="True">

            <hc:TabItem Header="相机配置">
                <views:CameraTabView/>
            </hc:TabItem>

            <hc:TabItem Header="PLC配置">
                <views:PlcTabView/>
            </hc:TabItem>

            <hc:TabItem Header="数据库配置">
                <views:DatabaseTabView/>
            </hc:TabItem>

            <hc:TabItem Header="Vm方案配置">
                <views:SolutionTabView/>
            </hc:TabItem>

            <hc:TabItem Header="存图配置">
                <views:SaveImageTabView/>
            </hc:TabItem>

            <hc:TabItem Header="Mes配置">
                <views:MesTabView/>
            </hc:TabItem>

            <hc:TabItem Header="通用设置">
                <views:GeneralTabView/>
            </hc:TabItem>

        </hc:TabControl>
    </Grid>
</Window>
